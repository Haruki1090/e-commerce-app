type ProductType = {
    id: number;
    title: string;
    description: string;
    thumbnail: {url: string};
    price: number;
    createdAt: string;
    updatedAt: string;
};

export type { ProductType };
